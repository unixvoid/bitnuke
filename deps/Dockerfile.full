FROM alpine

ENV DIFF=<DIFF>

RUN apk update && apk add redis
RUN mkdir -p /cryo/log
RUN rm -rf /var/cache/apk/*

COPY conf /cryo/conf
COPY data /cryo/data
COPY bitnuke /bin/
COPY config.gcfg /
COPY nginx /bin/
COPY run.sh /

CMD ["/run.sh"]
